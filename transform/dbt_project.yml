name: osprey
version: "0.1.0"
profile: osprey

model-paths: ["models"]
macro-paths: ["macros"]
target-path: "target"
clean-targets: ["target", "dbt_packages"]

models:
  osprey:
    silver:
      +materialized: view
      +schema: silver
    gold:
      +schema: gold

seeds:
  osprey:
    +schema: reference     # version-controlled reference data (ISO country names)
